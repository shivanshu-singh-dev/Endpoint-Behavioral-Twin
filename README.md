# Endpoint-Behavioral-Twin
EBT runs submitted files inside a safe VM twin, collect multi-sensor telemetry (file, process, persistence, network, config), score &amp; report behavior, and demonstrate detection of malicious patterns using safe simulations.
